#include<stdio.h>

int main()
{
    // initial condition (starting point)
    // final condition (termination/ending point)
    // operation (++/--)

    int i = 10; // initial condition
    while(i>=1){
        printf("%d ",i);
        i--;
    }
    return 0;
}