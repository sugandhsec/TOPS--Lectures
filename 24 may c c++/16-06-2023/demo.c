// manage--->user snippets--->c.json and paste the given code
// seetings--->search zoom--->check mouse wheel zoom

// ctrl+Mouse wheel
//  word wrap--->settings--->search wrap -->word wrap option select on

// settings---search (Format)--->check format on paste,format on save

// settings---?search(integrated:font size)---and select the size of the fonr of terminal
#include <stdio.h>
int main()
{
    int num = 48;
    if (num > 25)
    {
        printf("Great");
        if (num < 0)

        {
            Printf("demo");
        }
    }
    return 0;
}